<div class="row">
	<div class="col-md-9">
		<% @posts.each_with_index do |post, index| %>
			<% if index == 0 %>
				<% css_class = "class=sml-margin-top" %>
			<% end if %>

			<div class="post">
				<h3 <%= css_class %>><strong><%= post.title %></strong>
					<small>
						<%= l post.created_at, format: :short %>
						<% post.labels.each do |label| %>
							<span class="label label-primary">
								<%= Topic.find(label.topic_id).abbrev %>
							</span>
						<% end %>
					</small>
				</h3>
				<%= raw(post.text) %>						
			</div>
		<% end %>				
	</div>
	<div class="col-md-3">
		<div class="twitter">
			<a href="https://twitter.com/chillinbob" class="twitter-follow-button" data-show-count="false" data-lang="en">Follow @chillinbob</a>
		</div>
		<div class="g-follow" data-annotation="bubble" data-height="20" data-href="//plus.google.com/112588393871548900625" data-rel="author"></div>
		<div>
		<p>Andecoded.com is constantly being developed and changed. Here are the 5 latest commits to the website according to GitHub</p>
			<h4 id="commits-header">Commits</h4>
			<ul id="commits">
			</ul>
		</div>
	</div>
</div>